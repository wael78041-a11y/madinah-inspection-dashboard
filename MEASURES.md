# مرجع المقاييس (DAX)

كل المقاييس مكتوبة في الموديل مع وصفها. هذا المرجع مولّد من ملفات TMDL.

## fact_visit

### عدد الزيارات

> إجمالي عدد الزيارات الرقابية

```dax
عدد الزيارات =
COUNTROWS('fact_visit')
```

تنسيق: `#,0`

### عدد الزيارات المكتملة

> الزيارات المنتهية فعليًا

```dax
عدد الزيارات المكتملة =
CALCULATE([عدد الزيارات], 'fact_visit'[حالة_مبسطة] = "مكتملة")
```

تنسيق: `#,0`

### نسبة الإنجاز

> الزيارات المكتملة ÷ إجمالي الزيارات

```dax
نسبة الإنجاز =
DIVIDE([عدد الزيارات المكتملة], [عدد الزيارات])
```

تنسيق: `0.0%`

### عدد المنشآت

> عدد المنشآت الفريدة المزارة (مفتاح الرخصة)

```dax
عدد المنشآت =
DISTINCTCOUNT('fact_visit'[رقم الرخصة])
```

تنسيق: `#,0`

### عدد المراقبين

> عدد المراقبين النشطين

```dax
عدد المراقبين =
DISTINCTCOUNT('fact_visit'[هوية المراقب])
```

تنسيق: `#,0`

### متوسط درجة الامتثال

> متوسط درجة الامتثال (0-100)

```dax
متوسط درجة الامتثال =
AVERAGE('fact_visit'[درجة الامتثال])
```

تنسيق: `#,0.0`

### متوسط مدة الزيارة (دقيقة)

> يستبعد تلقائيًا المدد السالبة والأطول من 24 ساعة

```dax
متوسط مدة الزيارة (دقيقة) =
AVERAGE('fact_visit'[مدة الزيارة (دقيقة)])
```

تنسيق: `#,0.0`

### متوسط زمن الاستجابة (ساعة)

> من الإسناد إلى بدء الزيارة

```dax
متوسط زمن الاستجابة (ساعة) =
AVERAGE('fact_visit'[زمن الاستجابة (ساعة)])
```

تنسيق: `#,0.0`

### عدد الزيارات الممتثلة

> زيارات بلا أي مخالفة

```dax
عدد الزيارات الممتثلة =
COUNTROWS(FILTER('fact_visit', ISBLANK(CALCULATE(COUNTROWS('fact_violation')))))
```

تنسيق: `#,0`

### نسبة الامتثال

> الزيارات بلا مخالفات ÷ إجمالي الزيارات

```dax
نسبة الامتثال =
DIVIDE([عدد الزيارات الممتثلة], [عدد الزيارات])
```

تنسيق: `0.0%`

### زيارات لكل مراقب

> متوسط عبء العمل

```dax
زيارات لكل مراقب =
DIVIDE([عدد الزيارات], [عدد المراقبين])
```

تنسيق: `#,0.0`

### عدد الزيارات ش.س

> عدد الزيارات في الشهر السابق

```dax
عدد الزيارات ش.س =
CALCULATE([عدد الزيارات], DATEADD('dim_date'[التاريخ], -1, MONTH))
```

تنسيق: `#,0`

### تغير الزيارات %

> نسبة التغير عن الشهر السابق

```dax
تغير الزيارات % =
VAR p = [عدد الزيارات ش.س]
RETURN DIVIDE([عدد الزيارات] - p, p)
```

تنسيق: `0.0%`

## fact_violation

### عدد المخالفات

> عدد صفوف المخالفات — لا تستخدم DISTINCTCOUNT على معرف المخالفة لأنه يتصادم

```dax
عدد المخالفات =
COUNTROWS('fact_violation')
```

تنسيق: `#,0`

### إجمالي الغرامات

> إجمالي قيم المخالفات — يُحسب على حبيبة المخالفة

```dax
إجمالي الغرامات =
SUM('fact_violation'[قيمة المخالفة])
```

تنسيق: `#,0`

### متوسط الغرامة

> متوسط قيمة المخالفة الواحدة

```dax
متوسط الغرامة =
AVERAGE('fact_violation'[قيمة المخالفة])
```

تنسيق: `#,0`

### الغرامات المسددة

> قيمة الغرامات المسددة فعليًا

```dax
الغرامات المسددة =
CALCULATE([إجمالي الغرامات], 'fact_violation'[حالة السداد] = "مسددة")
```

تنسيق: `#,0`

### الغرامات غير المسددة

> قيمة الغرامات المستحقة

```dax
الغرامات غير المسددة =
CALCULATE([إجمالي الغرامات], 'fact_violation'[حالة السداد] = "لم تُسدد")
```

تنسيق: `#,0`

### نسبة التحصيل

> المسددة ÷ (المسددة + غير المسددة) — تستبعد الملغاة وغير المفوترة

```dax
نسبة التحصيل =
DIVIDE([الغرامات المسددة], [الغرامات المسددة] + [الغرامات غير المسددة])
```

تنسيق: `0.0%`

### عدد الإنذارات

> المخالفات المصنفة كإنذار

```dax
عدد الإنذارات =
CALCULATE([عدد المخالفات], 'fact_violation'[هل المخالفة انذار] = "نعم")
```

تنسيق: `#,0`

### مخالفات لكل زيارة

> متوسط عدد المخالفات في الزيارة

```dax
مخالفات لكل زيارة =
DIVIDE([عدد المخالفات], [عدد الزيارات])
```

تنسيق: `#,0.00`

### إجمالي الغرامات ش.س

> إجمالي الغرامات في الشهر السابق

```dax
إجمالي الغرامات ش.س =
CALCULATE([إجمالي الغرامات], DATEADD('dim_date'[التاريخ], -1, MONTH))
```

تنسيق: `#,0`

### تغير الغرامات %

> نسبة التغير عن الشهر السابق

```dax
تغير الغرامات % =
VAR p = [إجمالي الغرامات ش.س]
RETURN DIVIDE([إجمالي الغرامات] - p, p)
```

تنسيق: `0.0%`
